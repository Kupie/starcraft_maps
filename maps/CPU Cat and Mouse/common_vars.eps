var currentRound       = EUDVariable(0);

var delayBetweenSpawns = EUDVariable(64); // How many frames between spawning units

var realTimeSecondsPerRound = 38; // How many seconds should each round last, when running on "fastest" speed

function beforeTriggerExec() {

	
}